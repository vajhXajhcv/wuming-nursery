{
  "name": "wuming-nursery",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build:kaoyan": "node scripts/build-kaoyan.js",
    "build:triworld": "node scripts/build-triworld.js",
    "check:tools": "node scripts/check-md2docx.mjs",
    "build": "npm run build:kaoyan && npm run build:triworld && npm run check:tools && astro build && node scripts/pack-paid-content.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "upload:content": "node scripts/upload-paid-content.mjs",
    "backup": "node scripts/backup-kv.mjs",
    "deploy": "npm run build && npm run upload:content && wrangler pages deploy",
    "pages:dev": "npm run build && wrangler pages dev ./dist"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.2.2",
    "sharp": "^0.34.3"
  }
}