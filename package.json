{
  "name": "wuming-nursery",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build:kaoyan": "node scripts/build-kaoyan.js",
    "build": "npm run build:kaoyan && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy",
    "pages:dev": "npm run build && wrangler pages dev ./dist"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.2.2",
    "sharp": "^0.34.3"
  }
}