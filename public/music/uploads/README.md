# 上传自己的音乐

把音频文件放进这个目录，构建时会自动扫描并加入全站音乐播放器。

- 支持格式：mp3 / ogg / m4a / flac / wav
- 命名约定：`曲名 - 艺术家.mp3`（` - ` 分隔；艺术家可省略，省略后显示为「我的上传」）
- 例：`月光 - 德彪西.mp3`、`my-recording.mp3`
- 改完执行 `npm run deploy` 重新部署后生效

注意：请只上传你有权公开传播的音乐（自己的作品、公有领域或 CC 授权曲目）。此目录内容会进入 git 仓库并公开上线，注意文件体积。
